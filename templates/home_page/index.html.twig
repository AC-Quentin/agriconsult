{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}

{% block body %}

  <div class="container-fluid mb-4">
    <div class="row">
      <div class="col-md-3 mt-3">
        {{ include('sidebar.html.twig') }}
      </div>
      <div class="col-md-8">

  {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COMMERCIALE') %}
      {% if app.request.query.get('demandeId') is not null %}
      <div class="alert alert-success mt-1" role="alert">
        Demande commerciale créée avec succès ! ID : {{ app.request.query.get('demandeId') }}
      </div>
    {% endif %}
    <div class="container h-100">
      <div class="row align-middle">
        <div class="col-md-6 col-lg-4 column">
        <a href="{{ path ('app_demande_commerciale_stockage') }}" class="nav-link">
          <div class="card gr-1">
            <div class="txt">
              <h1>NOUVEAU -</br> STOCKAGE</h1>
              <p>Demande commerciale pour une ou plusieurs cellules de stockage</p>
            </div>
            <div class="ico-card">
            <i><img src="{{ asset('imgs/stockage.png') }}" alt="logo" height="200"></i>
          </div>
          </div>
        </a>
        </div>

        <div class="col-md-6 col-lg-4 column">
          <a href="{{ path ('app_demande_commerciale_secheuse') }}" class="nav-link">
            <div class="card gr-2">
              <div class="txt">
                <h1>NOUVELLE -</br> SECHEUSE</h1>
                <p>Demande commerciale pour une cellule Sécheuse</p>
              </div>
            <div class="ico-card">
              <i><img src="{{ asset('imgs/secheuse.png') }}" alt="logo" height="200"></i>
            </div>
            </div>
          </a>
        </div>

        <div class="col-md-6 col-lg-4 column">
          <div class="card gr-3">
            <div class="txt">
              <h1>NOUVEL ENSEMBLE - </br> STOCKAGE-SECHEUSE</h1>
              <p>Ensemble stockage, séchage, Vis fixe/mobile</p>
            </div>
            <div class="ico-card">
              <i><img src="{{ asset('imgs/silo.png') }}" alt="logo" height="200"></i>
            </div>
          </div>
        </div>
        
      </div>
      <div class="row align-middle">
        
        <div class="col-12 column">
          <a href="#" class="nav-link">
            <div class="card gr-4">
              <div class="txt">
                <h1>NOUVEL ENSEMBLE - </br> Personnalisé</h1>
                <p>Ensemble stockage, séchage, manutention, ...</p>
              </div>
              <div class="ico-card">
                <i><img src="{{ asset('imgs/personnalise.png') }}" alt="logo" height="270"></i>
              </div>
            </div>
          </a>
        </div>

      </div>
    </div>

  {% endif %}
        </div>
  

    </div>
  </div>

{% if is_granted('ROLE_ADMIN') %}
 {% if task_in_progress is not empty %}
     <div class="card mb-4">
      <div class="card-header">
        <h4 style="color: #F75E1E;">Tâches en cours</h4>
      </div>
      <div class="card-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th style="color: #F75E1E; font-size: 0.9em;"># Nom de la tâche</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Dpt</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Responsable</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Type de projet</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Echéance</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Priorité</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Progression</th>
                </tr>
            </thead>
            <tbody>

                {% for task in task_in_progress %}
                    <tr {% if task.completed %}class="table-success"{% endif %}>
                        <td class="small text-muted">{{ task.name }}</td>
                        <td class="small text-muted">
                            {% set dptValue = '' %}
                            {% for field in task.custom_fields %}
                                {% if field.name == 'Dpt' and field.display_value is not empty %}
                                    {% set dptValue = field.display_value %}
                                {% endif %}
                            {% endfor %}
                            {{ dptValue }}
                        </td>
                        <td class="small text-muted">
                            {% if task.assignee is defined %}
                                {{ task.assignee.name }}
                            {% else %}
                                Aucun responsable
                            {% endif %}
                        </td>
                        <td class="small text-muted">
                          {% set typeProjetValue = '' %}
                          {% for field in task.custom_fields %}
                              {% if field.name == 'Type Projet' and field.display_value is not empty %}
                                  {% set typeProjetValue = field.display_value %}
                              {% endif %}
                          {% endfor %}
                           {% if typeProjetValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if typeProjetValue == 'Standard' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Manutention' %}
                                    {% set badgeClass = 'bg-danger' %}
                                {% elseif typeProjetValue == 'Nettoyage' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif typeProjetValue == 'Séchage/Stockage' %}
                                    {% set badgeClass = 'bg-secondary' %}
                                {% elseif typeProjetValue == 'Plan' %}
                                    {% set badgeClass = 'bg-primary' %}
                                {% elseif typeProjetValue == 'Nettoyage/Stockage' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif typeProjetValue == 'Triage' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Stockage/Manutention' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Sécheuse/Manutention' %}
                                    {% set badgeClass = 'bg-light' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}
                              <span class="badge rounded-pill {{ badgeClass }}">{{ typeProjetValue }}</span>
                          {% else %}
                              Aucun type de projet
                          {% endif %}
                      </td>
                        <td class="small text-muted">
                            {% set dueDateValue = task.due_on ? task.due_on|date('d-m-Y') : 'Aucune échéance' %}
                            <span class="small text-muted">{{ dueDateValue }}</span>
                        </td>
                        <td class="small text-muted">
                          {% set priorityValue = '' %}
                          {% for field in task.custom_fields %}
                              {% if field.name == 'Priorité' and field.enum_value is not empty %}
                                  {% set priorityValue = field.enum_value.name %}
                              {% endif %}
                          {% endfor %}

                          {% if priorityValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if priorityValue == 'Élevée' %}
                                    {% set badgeClass = 'bg-danger' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}

                              <span class="badge rounded-pill {{ badgeClass }}">{{ priorityValue }}</span>
                          {% else %}
                              <span class="badge rounded-pill"></span>
                          {% endif %}
                      </td>
                        <td class="small text-muted">
                            {% set progressionValue = '' %}
                            {% for field in task.custom_fields %}
                                {% if field.name == '📈 Progression' and field.display_value is not empty %}
                                    {% set progressionValue = field.display_value %}
                                {% endif %}
                            {% endfor %}
                            
                            {% if progressionValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if progressionValue == 'En attente' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif progressionValue == 'Attente Electricité' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif progressionValue == 'Non Commencé' %}
                                    {% set badgeClass = 'bg-info' %}
                                {% elseif progressionValue == 'Terminé' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif progressionValue == 'En cours Méca' %}
                                    {% set badgeClass = 'bg-secondary' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}
                                
                                <span class="badge rounded-pill {{ badgeClass }}">{{ progressionValue }}</span>
                            {% else %}
                                <span class="badge rounded-pill">Aucune progression</span>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}

            </tbody>
        </table>
      </div>
    </div>
  {% else %}
    <div class="alert alert-info" role="alert">
        Aucune tâche en cours à afficher.
    </div>
  {% endif %}
{% endif %}

{% if is_granted('ROLE_COMMERCIALE') %}
  {% if project_in_progress is not empty %}
          <!-- En attente d'intégration -->
    <div class="card mb-4">
      <div class="card-header">
        <h4 style="color: #F75E1E;">En attente d'intégration</h4>
      </div>
      <div class="card-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th style="color: #F75E1E; font-size: 0.9em;"># Nom de la tâche</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Dpt</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Responsable</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Type de projet</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Echéance</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Priorité</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Progression</th>
                </tr>
            </thead>
            <tbody>
                {% for task in project_in_progress %}
                    {% set inIntegration = false %}
                    {% for membership in task.memberships %}
                    {% for section in membership %}
                        {% if section.name == 'En Attente d\'Intégration' %}
                        {% set inIntegration = true %}
                        {% endif %}
                    {% endfor %}
                    {% endfor %}

                    {% if inIntegration %}
                        <tr {% if task.completed %}class="table-success"{% endif %}>
                            <td class="small text-muted">{{ task.name }}</td>
                            <td class="small text-muted">
                              {% set dptValue = '' %}
                              {% for field in task.custom_fields %}
                                  {% if field.name == 'Dpt' and field.display_value is not empty %}
                                      {% set dptValue = field.display_value %}
                                  {% endif %}
                              {% endfor %}
                              {{ dptValue }}
                            </td>
                            <td class="small text-muted">
                              {% if task.assignee is defined and task.assignee is not null %}
                                  {{ task.assignee.name }}
                              {% else %}
                                  Aucun responsable
                              {% endif %}
                            </td>
                        <td class="small text-muted">
                          {% set typeProjetValue = '' %}
                          {% for field in task.custom_fields %}
                              {% if field.name == 'Type Projet' and field.display_value is not empty %}
                                  {% set typeProjetValue = field.display_value %}
                              {% endif %}
                          {% endfor %}
                           {% if typeProjetValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if typeProjetValue == 'Standard' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Manutention' %}
                                    {% set badgeClass = 'bg-danger' %}
                                {% elseif typeProjetValue == 'Nettoyage' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif typeProjetValue == 'Séchage/Stockage' %}
                                    {% set badgeClass = 'bg-secondary' %}
                                {% elseif typeProjetValue == 'Plan' %}
                                    {% set badgeClass = 'bg-primary' %}
                                {% elseif typeProjetValue == 'Nettoyage/Stockage' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif typeProjetValue == 'Triage' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Stockage/Manutention' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Sécheuse/Manutention' %}
                                    {% set badgeClass = 'bg-light' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}
                              <span class="badge rounded-pill {{ badgeClass }}">{{ typeProjetValue }}</span>
                          {% else %}
                              Aucun type de projet
                          {% endif %}
                      </td>
                      <td class="small text-muted">
                          {% set dueDateValue = task.due_on ? task.due_on|date('d-m-Y') : 'Aucune échéance' %}
                          <span class="small text-muted">{{ dueDateValue }}</span>
                      </td>
                      <td class="small text-muted">
                          {% set priorityValue = '' %}
                          {% for field in task.custom_fields %}
                              {% if field.name == 'Priorité' and field.enum_value is not empty %}
                                  {% set priorityValue = field.enum_value.name %}
                              {% endif %}
                          {% endfor %}

                          {% if priorityValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if priorityValue == 'Élevée' %}
                                    {% set badgeClass = 'bg-danger' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}

                              <span class="badge rounded-pill {{ badgeClass }}">{{ priorityValue }}</span>
                          {% else %}
                              <span class="badge rounded-pill"></span>
                          {% endif %}
                      </td>
                        <td class="small text-muted">
                            {% set progressionValue = '' %}
                            {% for field in task.custom_fields %}
                                {% if field.name == '📈 Progression' and field.display_value is not empty %}
                                    {% set progressionValue = field.display_value %}
                                {% endif %}
                            {% endfor %}
                            
                            {% if progressionValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if progressionValue == 'En attente' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif progressionValue == 'Attente Electricité' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif progressionValue == 'Non Commencé' %}
                                    {% set badgeClass = 'bg-info' %}
                                {% elseif progressionValue == 'Terminé' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif progressionValue == 'En cours Méca' %}
                                    {% set badgeClass = 'bg-secondary' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}
                                
                                <span class="badge rounded-pill {{ badgeClass }}">{{ progressionValue }}</span>
                            {% else %}
                                <span class="badge rounded-pill">Aucune progression</span>
                            {% endif %}
                        </td>

                        </tr>
                    {% endif %}
                {% endfor %}
            </tbody>
        </table>
      </div>
    </div>
          <!-- Devis en cours -->
    <div class="card mb-4">
      <div class="card-header">
        <h4 style="color: #F75E1E;">Devis en cours</h4>
      </div>
      <div class="card-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th style="color: #F75E1E; font-size: 0.9em;"># Nom de la tâche</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Dpt</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Responsable</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Type de projet</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Echéance</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Priorité</th>
                    <th style="color: #F75E1E; font-size: 0.9em;">Progression</th>
                </tr>
            </thead>
            <tbody>
                {% for task in project_in_progress %}
                    {% set inDevis = false %}
                    {% for membership in task.memberships %}
                    {% for section in membership %}
                        {% if section.name == 'Devis en Cours' %}
                            {% set inDevis = true %}
                        {% endif %}
                    {% endfor %}
                    {% endfor %}

                    {% if inDevis %}
                        <tr {% if task.completed %}class="table-success"{% endif %}>
                            <td class="small text-muted">{{ task.name }}</td>
                            <td class="small text-muted">
                              {% set dptValue = '' %}
                              {% for field in task.custom_fields %}
                                  {% if field.name == 'Dpt' and field.display_value is not empty %}
                                      {% set dptValue = field.display_value %}
                                  {% endif %}
                              {% endfor %}
                              {{ dptValue }}
                            </td>
                            <td class="small text-muted">
                              {% if task.assignee is defined and task.assignee is not null %}
                                  {{ task.assignee.name }}
                              {% else %}
                                  Aucun responsable
                              {% endif %}
                            </td>
                        <td class="small text-muted">
                          {% set typeProjetValue = '' %}
                          {% for field in task.custom_fields %}
                              {% if field.name == 'Type Projet' and field.display_value is not empty %}
                                  {% set typeProjetValue = field.display_value %}
                              {% endif %}
                          {% endfor %}
                           {% if typeProjetValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if typeProjetValue == 'Standard' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Manutention' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif typeProjetValue == 'Nettoyage' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif typeProjetValue == 'Séchage/Stockage' %}
                                    {% set badgeClass = 'bg-secondary' %}
                                {% elseif typeProjetValue == 'Plan' %}
                                    {% set badgeClass = 'bg-primary' %}
                                {% elseif typeProjetValue == 'Nettoyage/Stockage' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif typeProjetValue == 'Triage' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Stockage/Manutention' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif typeProjetValue == 'Sécheuse/Manutention' %}
                                    {% set badgeClass = 'bg-info' %}
                                {% else %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% endif %}
                              <span class="badge rounded-pill {{ badgeClass }}">{{ typeProjetValue }}</span>
                          {% else %}
                              Aucun type de projet
                          {% endif %}
                      </td>
                      <td class="small text-muted">
                          {% set dueDateValue = task.due_on ? task.due_on|date('d-m-Y') : 'Aucune échéance' %}
                          <span class="small text-muted">{{ dueDateValue }}</span>
                      </td>
                      <td class="small text-muted">
                          {% set priorityValue = '' %}
                          {% for field in task.custom_fields %}
                              {% if field.name == 'Priorité' and field.enum_value is not empty %}
                                  {% set priorityValue = field.enum_value.name %}
                              {% endif %}
                          {% endfor %}

                          {% if priorityValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if priorityValue == 'Élevée' %}
                                    {% set badgeClass = 'bg-danger' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}

                              <span class="badge rounded-pill {{ badgeClass }}">{{ priorityValue }}</span>
                          {% else %}
                              <span class="badge rounded-pill"></span>
                          {% endif %}
                      </td>
                        <td class="small text-muted">
                            {% set progressionValue = '' %}
                            {% for field in task.custom_fields %}
                                {% if field.name == '📈 Progression' and field.display_value is not empty %}
                                    {% set progressionValue = field.display_value %}
                                {% endif %}
                            {% endfor %}
                            
                            {% if progressionValue is not empty %}
                                {# Ici, on change la couleur du badge en fonction de la valeur de progressionValue #}
                                {% set badgeClass = '' %}
                                {% if progressionValue == 'En attente' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif progressionValue == 'Attente Electricité' %}
                                    {% set badgeClass = 'bg-warning' %}
                                {% elseif progressionValue == 'Non Commencé' %}
                                    {% set badgeClass = 'bg-primary' %}
                                {% elseif progressionValue == 'Terminé' %}
                                    {% set badgeClass = 'bg-success' %}
                                {% elseif progressionValue == 'En cours Méca' %}
                                    {% set badgeClass = 'bg-secondary' %}
                                {% else %}
                                    {% set badgeClass = '' %}
                                {% endif %}
                                
                                <span class="badge rounded-pill {{ badgeClass }}">{{ progressionValue }}</span>
                            {% else %}
                                <span class="badge rounded-pill">Aucune progression</span>
                            {% endif %}
                        </td>
                        </tr>
                    {% endif %}
                {% endfor %}
            </tbody>
        </table>
    </div>
  </div>
  {% else %}
      <div class="alert alert-info" role="alert">
          Aucune tâche en cours à afficher.
      </div>
  {% endif %}
{% endif %}


  {% if app.request.query.get('mailto') is not null %}
      <script type="text/javascript">
          // Ouvre la fenêtre mailto
          window.location.href = "{{ app.request.query.get('mailto') }}";

          // Lance automatiquement le téléchargement du PDF
          window.location.href = "{{ path('telecharger_pdf') }}";
      </script>

  {% endif %}

{% endblock %}
